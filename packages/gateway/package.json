{
  "name": "@agent-arcade/gateway",
  "version": "3.2.0",
  "description": "Agent Arcade telemetry relay gateway — WebSocket + SSE + HTTP ingest",
  "private": true,
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "start": "bun src/index.ts",
    "test": "bun test test/"
  },
  "dependencies": {
    "@agent-arcade/notifications": "workspace:*",
    "@socket.io/redis-adapter": "^8.3.0",
    "jose": "^6.1.0",
    "redis": "^5.8.2",
    "socket.io": "^4.8.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.4",
    "@types/uuid": "^10.0.0"
  }
}
